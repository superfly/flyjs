{
  "name": "@fly-test/edge-apps",
  "version": "0.55.7",
  "private": true,
  "scripts": {
    "test": "jest --maxWorkers=1 --verbose"
  },
  "devDependencies": {
    "@fly/test-environment": "0.55.7",
    "jest": "^24",
    "jest-circus": "^24",
    "ts-jest": "^24"
  }
}
